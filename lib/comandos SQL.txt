INSERT:

INSERT INTO FUNCIONARIO (NOME_FUNC, CPF, ID_UNIDADE, ID_SERVICO, HORA_INICIO, HORA_FINAL) values ('Cesar Cordeiro de Quadros','37765289829','1','1','08:00','18:00');

UPDATE:

UPDATE FUNCIONARIO SET ID_UNIDADE = '2', ID_SERVICO = '3' WHERE CPF = '42546810898';



UPDATE STATUS SET NOME_STATUS = 'Vago' WHERE ID_STATUS = '1';


CREATE TABLE `AGENDAMENTO` (
	`ID_AGENDAMENTO`	INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
	`ID_FUNC`	INTEGER NOT NULL,
	`ID_CLIENTE`	INTEGER NOT NULL,
	`STATUS`	VARCHAR(50) NOT NULL,
	`ID_SERVICO`	INTEGER NOT NULL,
	`ID_UNIDADE`	INTEGER NOT NULL,
	`DATA_AGENDA`	DATE NOT NULL,
	`HORA_INICIO_AGENDA`	TIME(7) NOT NULL,
	`HORA_FINAL_AGENDA`	TIME(7) NOT NULL,
	FOREIGN KEY(`ID_FUNC`) REFERENCES `FUNCIONARIO`(`ID_FUNC`),
	FOREIGN KEY(`ID_CLIENTE`) REFERENCES `CLIENTE`(`ID_CLIENTE`),
	FOREIGN KEY(`STATUS`) REFERENCES `STATUS`(`ID_STATUS`),
	FOREIGN KEY(`ID_SERVICO`) REFERENCES `SERVICO`(`ID_SERVICO`),
	FOREIGN KEY(`ID_UNIDADE`) REFERENCES `UNIDADE`(`ID_UNIDADE`)
);


SELECT F.NOME [NOMEFUNC], F.ID_SERVICO AS [FUNCIDSERVICO] FROM FUNCIONARIO AS F
   INNER JOIN SERVICO AS S
     ON F.ID_SERVICO = 1;